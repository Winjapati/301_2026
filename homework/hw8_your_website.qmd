---
title: "Homework 8: Your Website"
subtitle: "LIN 301 — Personal Website"
format:
  html:
    toc: true
    toc-depth: 2
    code-fold: false
    smooth-scroll: true
    theme: cosmo
---

<!-- PRELIMINARY: adapted from the old HW_2_website.qmd. Key changes: RStudio -> VSCode steps, due date moved to the end of the semester (this used to be a mid-course standalone HW; now it's the last of the 8 and doubles as the home for the final project), and added a required "project" section since this site now needs to actually host/link the final project for the Week 16 presentations. -->

## Overview

You'll publish a polished personal website using everything we practiced across **HTML → CSS → Baby JavaScript → GitHub Pages** — and this time, it's not just a portfolio exercise. This site is where you'll host and present your **final project** in Week 16.

If you've been keeping up, this shouldn't take you too long: focus on **styling and clarity**, not adding new features or complicated code.

**Deliverable:** a public URL to your site (hosted via GitHub Pages).

**Due:** December 4, 2026

------------------------------------------------------------------------

## Required Elements (building on what you've already done)

### HTML structure

-   Semantic layout: `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`.
-   Internal anchors: a working **Back to top** link (e.g., `<a href="#top">…</a>` with `<a id="top"></a>` at top).
-   At least **5 sections** (e.g., About, Education, Projects, **Final Project**, Contact).
-   One image with descriptive `alt` text.
-   A link to your **GitHub profile**.

### Your Final Project Section

-   A dedicated section presenting your final course project:
    -   A short (3–5 sentence) summary of your dataset and analysis
    -   A link to your project's GitHub repo
    -   At least one visualization from your project, embedded as an image
-   This is what you'll pull up during your presentation in Week 16 — make sure it's finished before then.

### CSS

-   Use a separate **`style.css`**, linked in `<head>`.
-   **Custom properties (CSS variables)** in `:root` for color and spacing (e.g., `--ink`, `--paper`, `--accent`, `--space-2`).
-   **Class selectors** you created during class (e.g., `.tagline`, `.card`, `.btn`) and reuse at least one class in 2+ places.
-   **Flexbox** for the nav bar (`display: flex;` + `gap` + `justify-content`).
-   **Sticky header** (`position: sticky; top: 0;`) so the nav stays visible.
-   At least **two** subtle polish touches, such as:
    -   `rgba()` for soft hover/background tints,
    -   `calc()` for spacing math,
    -   improved list readability (spacing or markers).

### Pseudo-classes (light interaction)

-   At least **two** (choose from): `:hover`, `:focus-visible`, `:visited`, `:first-child`, `:last-child`, `:nth-child(...)`.
    -   Examples: (a) when hovering over nav links, make a visible change to the link; (b) when navigating with a keyboard and tabbing through sections/links, make sure focus is visibly highlighted in some way.

### Tiny JavaScript (pick one)

-   **Auto year** in footer;
-   **Dark Mode** toggle;
-   There are quite a few JS scripts posted online; you can surprise me!

### Keeping Things Accessible

-   Color contrast is readable (no light gray on white, etc.).
-   Images have informative alt text.
-   One `<h1>` per page; section headings use `<h2>`.
-   Links are descriptive (avoid "click here").

------------------------------------------------------------------------

## Posting Your Site Online

1.  Finalize your HTML/CSS/JS locally in VSCode.
2.  Place files in `docs` (remember that your homepage should be `index.html`!).
3.  Stage → Commit → Push your changes from VSCode's Source Control panel (or `git add` / `git commit` / `git push` in the integrated terminal).
4.  Your repo name must be: `yourusername.github.io`
5.  Confirm Pages is enabled: GitHub → **Settings → Pages** → Branch: **main**, Folder: **/docs** → **Save**.
6.  Wait ~1 minute; test your public link.
7.  Submit your URL to Canvas — you'll use this same link for your Week 16 presentation.
