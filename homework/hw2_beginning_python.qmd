---
title: "Homework 2: Beginning Python"
subtitle: "LIN 301: Computation for Linguists"
author: "Your Name"
format:
  html:
    toc: true
    toc-depth: 2
    code-tools: true
jupyter: python3
fontsize: 12pt
---

<!-- PRELIMINARY: split off from the old combined "hw3_terminal_python.qmd" (4_beginning_python version, which was the more polished of the two duplicates). This is the Python-only half; the terminal setup is HW1. -->

# Overview

**Goal:** Practice lists and dictionaries using real text data.

**Deliverable:** one Quarto file, `hw2.qmd`.

**Due:** September 16, 2026

---

# Part 1: Load the File

You should already have `shakespeare.txt` from HW1, in `homework/hw1_files/`. Reuse it here — no need to re-download.

In the first cell of your `hw2.qmd`, run:

```python
with open("hw1_files/shakespeare.txt", "r", encoding="utf-8") as f:
    text = f.read()
```

# Part 2: Make a List

- Using `.split()[:1000]`, take the first 1000 words (in the order they appear) and store them in a list called `first_words`.
- Print the list.
- Print the 500th item in the list.

# Part 3: Narrowing Down the List

- Pick **10 of the words** from your `first_words` list. Choose a mix of super common ones (like "the") and rarer ones.
- Manually place these 10 words in a new list called `chosen_ones`.

# Part 4: Make a Dictionary

- Create a third list, called `bill`, containing *all* of the words in `shakespeare.txt` (use `.split()` on the full text).
- Use `.count()` to see how many times each word in `chosen_ones` appears in `bill`.
- Create a dictionary called `word_counts` that pairs each chosen word with its count.
- Do **not** manually type in the counts — use the format `"word": bill.count("word")`.

---

# Part 5: Reflection

At the bottom of `hw2.qmd`, write a short (3–5 sentence) reflection:

- What was easiest about this assignment?
- What was hardest?
- How might a linguist use lists and dictionaries for language data?

---

# Submission

- Commit and push `hw2.qmd` to your `LIN_301` repo.
- Post the repo link to Canvas.
